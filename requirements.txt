numpy>=2.0.0,<2.1.0
scipy>=1.14.0
matplotlib
jupyter
scikit-learn
pandas

tqdm

websockets>=15.0.1

keras>=3.5.0,<3.6.0
tensorflow==2.18.1
grpcio-tools

onnxruntime

bottle
gevent
pythonnet>=3.0.4
bottle_session
flask
flask_cors
flask_limiter 
psutil==5.9.0
colorama

# --- Windows frozen-build deps only -----------------------------------------
# These are needed only to produce the Windows .exe bundles (install/*.spec) and
# are NOT used by the Linux/Docker runtime. They carry a `sys_platform == win32`
# marker so pip skips them on Linux (pywin32 has no Linux wheel and would fail
# the Docker build otherwise).

# Build tool — frozen .exe builds (install/*.spec). Pinned so the conda `ben`
# env is self-contained and reproduces the known-good bundle hooks.
pyinstaller==6.10.0 ; sys_platform == "win32"
# setuptools 71+ moved pkg_resources' vendored deps (jaraco.*, more_itertools)
# in a way PyInstaller 6.10's pkg_resources hook can't bundle, so frozen exes
# crash at the pyi_rth_pkgres runtime hook with "No module named 'jaraco'".
# 70.x keeps pkg_resources self-contained (vendored jaraco under _vendor). Pin <71.
setuptools==70.3.0 ; sys_platform == "win32"

# TMCGUI (table manager GUI) dependencies. pywin32 was missing entirely, so the
# frozen TMCGUI.exe shipped without win32gui and crashed at startup with
# "No module named 'win32gui'" despite TMCGUI.spec declaring it as a hidden import.
pywin32 ; sys_platform == "win32"
pygame ; sys_platform == "win32"